# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
.python-version

# MLflow experiment data (large, regenerated by training)
mlruns/

# Runtime artifacts
feedback.db
shap_summary_estimate.png

# IDE
.idea/

# CatBoost training logs
catboost_info/

# Embeddings and models — large binary artifacts, regenerated by training scripts
embeddings/*.npy
models/

# Data — full CSV files contain NDA data and are kept locally only.
# Committed stubs (data/*.csv) contain headers only and are protected
# via `git update-index --skip-worktree`. New data files are blocked here.
data/**/*.csv
data/*.csv.gz
data/*.parquet

# uv lock (optional — include if you want reproducible installs across machines)
# uv.lock
